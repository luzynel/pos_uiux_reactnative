import {StyleSheet} from 'react-native';
import {color} from '../../assets/utils/colors';
import {font, size} from '../../assets/utils/fonts';

const PendingOrdersStyles = StyleSheet.create({
  tblHeadCell: {
    textAlign: 'center',
    width: 100,
  },
  tblCell: {
    textAlign: 'center',
    width: 100,
  }
});

export default PendingOrdersStyles;
